---
import ZenThemeProvider from '../components/ZenThemeProvider.astro';
import ZenFooter from '../components/ZenFooter.astro';
import ZenTopBar from '../components/ZenTopBar.astro';
import '../styles/zenwave.css';

interface Props {
  title: string;
  description?: string;
}

const { title, description } = Astro.props;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title} | ZenWave 360</title>
    {description && <meta name="description" content={description} />}
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <ZenThemeProvider />
    <script
      data-goatcounter="https://zenwave360.goatcounter.com/count"
      async
      src="//gc.zgo.at/count.js"></script>
  </head>
  <body>
    <ZenTopBar />
    <slot />
    <ZenFooter />
  </body>
</html>

<style>
  :root {
    color-scheme: dark;
    --bg: var(--sl-color-bg, #111318);
    --panel: #191d25;
    --text: var(--sl-color-white, #f3f5f8);
    --muted: var(--sl-color-gray-2, #b9c0ca);
    --accent: var(--zen-highlight, #ffe04b);
    --border: var(--sl-color-hairline, #303744);
  }

  :root[data-theme='light'] {
    color-scheme: light;
    --bg: #fffaf7;
    --panel: #ffffff;
    --text: #191713;
    --muted: #5f6670;
    --border: #e6ded8;
  }

  body {
    margin: 0;
    min-height: 100vh;
    color: var(--text);
    background: var(--bg);
    font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  }

  a {
    color: inherit;
  }
</style>
